<form action="{{products_path}}" method="get" class="navbar-form">
<div class="form-group">
  {{taxons_select}}
</div>
<div class="form-group">
  <input type="search" name="keywords" placeholder="{{translate text='search'}}" class="form-control" value="{{keywords}}">
</div>
<input class="btn btn-success" type="submit" value="{{translate text='search'}}">
</form>
